# Final Deployment Closeout

Last verified: 2026-06-24 13:51 IST.

## Active production sites

1. `taiyzun.com`
   - Live URL: `https://taiyzun.com/`
   - Secondary host: `https://www.taiyzun.com/`
   - Cloudflare Pages project: `taiyzun-com`
   - GitHub repository: `https://github.com/taiyzun/taiyzun.com`
   - Latest verified commit: `c31f8310be856a80a4f738062a44ae248b837a4a`
   - R2 bucket: `taiyzun-gallery`
   - Asset host: `https://assets.taiyzun.com`

2. `taj-mahal-movie.com`
   - Live URL: `https://www.taj-mahal-movie.com/`
   - Redirect host: `https://taj-mahal-movie.com/`
   - Cloudflare Pages project: `taj-mahal-movie-com`
   - GitHub repository: `https://github.com/taiyzun/taj-mahal-movie.com`
   - Latest verified commit: `9d272e3e15eea8cb3119d447a717d49748212edc`
   - R2 bucket: `taj-mahal-movie-media`
   - Asset host: `https://assets.taj-mahal-movie.com`

## Current architecture

- DNS and edge routing: Cloudflare.
- Static hosting: Cloudflare Pages.
- Media/object storage: Cloudflare R2 with custom asset hostnames.
- Source control: GitHub.
- Deployment trigger: push to `main`.
- Tai build output: `dist`, generated by `npm run build:pages`.
- Taj build output: repository root, with Cloudflare Pages build command `exit 0`.

## Deployment flow

### taiyzun.com

1. Edit source in `/Users/tai/Documents/GitHub/taiyzun.com`.
2. Run `npm run build`.
3. Run JavaScript syntax checks for changed scripts/functions.
4. Commit to `main`.
5. Push `main` to `https://github.com/taiyzun/taiyzun.com`.
6. Cloudflare Pages deploys project `taiyzun-com`.
7. Verify live routes and Pages noindex headers.

### taj-mahal-movie.com

1. Edit source in `/Users/tai/Documents/GitHub/taj-mahal-movie.com`.
2. Keep root files minimal because the repo root is deployed directly.
3. Commit to `main`.
4. Push `main` to `https://github.com/taiyzun/taj-mahal-movie.com`.
5. Cloudflare Pages deploys project `taj-mahal-movie-com`.
6. Verify live routes, apex redirect, 404 behavior, and Pages noindex headers.

## DNS and Cloudflare status

- `taiyzun.com` uses Cloudflare nameservers and resolves through Cloudflare.
- `taj-mahal-movie.com` uses Cloudflare nameservers and resolves through Cloudflare.
- Cloudflare Pages currently lists 2 active projects:
  - `taiyzun-com`
  - `taj-mahal-movie-com`
- Cloudflare R2 currently lists 2 active buckets:
  - `taiyzun-gallery`
  - `taj-mahal-movie-media`
- Old Cloudflare zones removed:
  - `shahpurwala.net`
  - `tajmahalmovies.com`
- `shahpurwala.net` did not resolve during final DNS spot-check.
- `tajmahalmovies.com` still resolves through GoDaddy parking/external DNS, not Cloudflare.
- `ep0ch.org` still resolves outside this active Cloudflare stack.

## Live verification checks

Latest spot-checks on 2026-06-24 returned:

- `https://taiyzun.com/` -> `200`
- `https://taiyzun.com/creations` -> `200`
- `https://taiyzun.com/robots.txt` -> `200`
- `https://taiyzun.com/sitemap.xml` -> `200`
- `https://taiyzun-com.pages.dev/` -> `200`
- `https://www.taj-mahal-movie.com/` -> `200`
- `https://www.taj-mahal-movie.com/robots.txt` -> `200`
- `https://www.taj-mahal-movie.com/sitemap.xml` -> `200`
- `https://taj-mahal-movie-com.pages.dev/` -> `200`

Previously verified cleanup checks:

- `https://taiyzun.com/CNAME` -> `404`
- `https://taiyzun.com/.htaccess` -> `404`
- `https://taiyzun.com/style.css.backup` -> `404`
- `https://taiyzun.com/seo-keywords.md` -> `404`
- `https://www.taj-mahal-movie.com/README.md` -> `404`
- Unknown Taj routes -> `404`
- Cloudflare Pages default hosts emit `X-Robots-Tag: noindex` where reachable.

## Lighthouse and CI status

- Workflow repository: `taiyzun/taiyzun.com`
- Workflow file: `.github/workflows/lighthouse.yml`
- Workflow name: `Lighthouse Audit`
- Branch: `main`
- Latest failing run inspected: `28069081061`
- Failing job: `lighthouse`
- Failure phase: before checkout; no workflow steps were started.
- GitHub check-run annotation: `The job was not started because your account is locked due to a billing issue.`
- Billable runtime reported by GitHub: `0 ms`.
- Authenticated `gh` status after refresh: valid, with `gist`, `read:org`, `repo`, and `workflow` scopes.

This is not caused by workflow syntax, checkout, Node setup, package installation, Lighthouse configuration, target URL availability, artifact upload, secrets, branch protection, or Cloudflare/live-site behavior. The blocker is GitHub account billing state.

After the GitHub billing lock is resolved, re-run:

```bash
gh run rerun 28069081061 --failed
gh run view 28069081061 --log-failed
```

If the rerun starts and then fails inside a workflow step, treat that as a new direct CI failure and fix only that next cause.

## Rollback method

### taiyzun.com

1. Identify the last known good commit:

```bash
git -C /Users/tai/Documents/GitHub/taiyzun.com log --oneline -10
```

2. Revert the bad commit, rebuild, and push:

```bash
cd /Users/tai/Documents/GitHub/taiyzun.com
git revert <commit>
npm run build
git push origin main
```

3. Wait for Cloudflare Pages project `taiyzun-com` to deploy.
4. Verify `https://taiyzun.com/`, `https://taiyzun.com/creations`, `robots.txt`, and `sitemap.xml`.

### taj-mahal-movie.com

1. Identify the last known good commit:

```bash
git -C /Users/tai/Documents/GitHub/taj-mahal-movie.com log --oneline -10
```

2. Revert the bad commit and push:

```bash
cd /Users/tai/Documents/GitHub/taj-mahal-movie.com
git revert <commit>
git push origin main
```

3. Wait for Cloudflare Pages project `taj-mahal-movie-com` to deploy.
4. Verify `https://www.taj-mahal-movie.com/`, apex redirect, `robots.txt`, `sitemap.xml`, and unknown-route `404`.

## Monthly maintenance checklist

- Confirm Cloudflare Pages still lists only `taiyzun-com` and `taj-mahal-movie-com`.
- Confirm R2 still lists only `taiyzun-gallery` and `taj-mahal-movie-media`.
- Check `gh auth status` before any GitHub Actions investigation.
- Check the Tai `Lighthouse Audit` status after GitHub billing is resolved.
- Run Tai `npm audit --omit=dev`.
- Run Tai `npm run build`.
- Verify key live routes for both sites.
- Confirm Pages default hosts remain noindexed.
- Confirm no private docs, setup notes, local reports, `.env` files, `.DS_Store`, `CNAME`, or backup files are served publicly.
- Review Cloudflare custom domains and DNS records for drift.
- Review GitHub branch protection and Actions health.
- Keep any billing/payment changes manual and owner-approved.

## Intentionally not touched

- No DNS changes were made during the Lighthouse close-out pass.
- No Cloudflare, registrar, hosting, or production settings were changed during the Lighthouse close-out pass.
- No site content or design was changed.
- No broad refactors were made.
- No dependency upgrades were made.
- No additional old domains were removed after the cleanup/deploy mission.
